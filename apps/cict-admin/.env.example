# Supabase project URL and anon key (public client usage)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# App URL for metadata / redirects
NEXT_PUBLIC_SITE_URL=http://localhost:3004

# Database connection for Prisma used by server actions
# Use a pooled connection for DATABASE_URL and direct connection for DIRECT_URL.
DATABASE_URL=
DIRECT_URL=
